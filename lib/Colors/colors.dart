import 'package:flutter/material.dart';

/// This class will hold all the app colors in one place.
/// You can call them anywhere in your app like: AppColors.iconColor
class AppColors {
  // Color for icons
  static const Color iconColor = Color(0xFF407BFF);

  // Color for buttons
  static const Color buttonColor = Color(0xFF407CE2);

  // Color for headings / titles
  static const Color headingColor = Color(0xFF221F1F);
}
